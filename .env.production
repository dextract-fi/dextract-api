# Dextract-fi API Production Environment Configuration

# Node environment
NODE_ENV=production

# API port (used for local testing of production build)
API_PORT=8787

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=https://dextract.xyz,https://app.dextract.xyz

# Debug mode
DEBUG=false